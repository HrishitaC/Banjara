<% include ../partials/header %>
 
<% spots_list = [] %>

<% spots.forEach(function(spot){ %>
      <% spots_list.push({name: spot.name, id: spot._id}); %>
<% }); %>

<form action="/hotels" method="POST">
   <div class="container">
      <h1 style="text-align: center; margin: 25px; "> Create a new hotel!</h1>
      <div class="row">
            <div style="width: 30%; margin: 0 auto;">
                  <input class="form-group form-control" type="text" name="name" placeholder="Hotel name">
                  <input class="form-group form-control" type="text" name="image" placeholder="Image URL">
                  <input class="form-group form-control" type="text" name="description" placeholder="Hotel decsription">
                  <p>Choose spots to be added (Hold CTRL / CMD for selecting multiple fields)</p>
                  <select name="addedSpots" class="custom-select" multiple>
                        <% spots_list.forEach(function(spot){ %>
                              <option value="<%= spot.id %>"><%= spot.name %></option>
                        <% }); %>
                  </select>
                  <hr>
                  <button class="btn btn-block btn-primary">SUBMIT!</button>
                  <a href="/hotels">Go back!</a>      
            </div>
      </div>
   </div>
</form>
 

<% include ../partials/footer %>